---
const projects = [
    {
        title: "MusicApp - Clon de Spotify",
        description:
            "Aplicación web de streaming musical inspirada en Spotify. Incluye biblioteca personal, búsqueda de música, canciones del momento, nuevos lanzamientos y categorías destacadas. Interfaz moderna y funcionalidades completas de reproducción.",
        image: "/MusicApp.png",
        tags: ["Astro", "JavaScript", "Tailwind CSS", "Solid.js"],
        link: "https://castor-prueba-tecnica.pages.dev/",
    },
    {
        title: "De Cero a Intérprete Médico",
        description:
            "Landing page profesional para un equipo de intérpretes especializados en entrevistas de USCIS. Incluye servicios de interpretación, cursos de formación y traducción de documentos certificados. Diseño moderno con secciones de servicios, programas educativos y equipo.",
        image: "/pamela.png",
        tags: ["Astro", "JavaScript", "Tailwind CSS", "Solid.js"],
        link: "https://deceroainterpretemedico.pages.dev/",
    },
    {
        title: "Agencia de Desarrollo Web",
        description:
            "Sitio web de agencia de desarrollo con servicios de desarrollo web y sitios web personalizados.",
        image: "/bombaweb.webp",
        tags: ["Astro", "JavaScript", "Tailwind CSS", "Solid.js"],
        link: "https://koaladevs.pages.dev/",
    },
    {
        title: "Portafolio Psicóloga",
        description:
            "Portafolio profesional con información sobre experiencia profesional y habilidades. Diseño elegante y moderno que destaca la trayectoria profesional.",
        image: "/portafolio.webp",
        tags: ["Astro", "JavaScript", "Tailwind CSS", "Solid.js"],
        link: "https://demo-portfolio-d1t.pages.dev/",
    },
];
---

<section id="projects" class="relative py-20 min-h-screen flex items-center justify-center overflow-hidden">
    <!-- Background effects -->
    <div class="absolute inset-0 bg-[radial-gradient(circle_at_80%_30%,rgba(59,130,246,0.08),transparent_50%)]"></div>
    <div class="absolute inset-0 bg-[linear-gradient(to_right,#80808008_1px,transparent_1px),linear-gradient(to_bottom,#80808008_1px,transparent_1px)] bg-[size:24px_24px]"></div>
    
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
        <!-- Section header -->
        <div class="text-center mb-16">
            <span class="inline-block px-4 py-2 rounded-full bg-primary/10 text-primary text-sm font-semibold mb-4">
                Portafolio
            </span>
            <h2 class="text-4xl sm:text-5xl md:text-6xl font-bold mb-4">
                <span class="text-white">Proyectos</span>
                <span class="block text-primary">
                    Destacados
                </span>
            </h2>
            <p class="text-xl text-gray-400 max-w-2xl mx-auto mb-4">
                Proyectos personales que demuestran mi experiencia y creatividad
            </p>
            <div class="inline-flex items-center gap-2 px-4 py-2 rounded-lg bg-gray-800/50 border border-gray-700/50 text-sm text-gray-300">
                <svg class="w-4 h-4 text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
                <span>He desarrollado múltiples proyectos internos en <span class="text-primary font-semibold">Janover</span>, <span class="text-primary font-semibold">DataScoring</span> y <span class="text-primary font-semibold">VMO Central</span> que no puedo mostrar por confidencialidad</span>
            </div>
        </div>

        <!-- Projects Grid -->
        <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6 lg:gap-8">
            {projects.map((project, index) => (
                <article 
                    class="group relative bg-gray-800/40 backdrop-blur-sm border border-gray-700/50 rounded-2xl overflow-hidden hover:border-primary/50 transition-all duration-500 hover:shadow-xl hover:shadow-primary/10 hover:-translate-y-2"
                    style={`animation-delay: ${index * 0.1}s;`}
                >
                    <!-- Glow effect -->
                    <div class="absolute -inset-1 bg-primary/20 rounded-2xl blur-xl opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
                    
                    <!-- Image container with full overlay -->
                    <div class="relative overflow-hidden h-80">
                        <img
                            src={project.image}
                            alt={project.title}
                            class="w-full h-full object-cover transform group-hover:scale-110 transition-transform duration-700"
                        />
                        
                        <!-- Gradient overlay (always visible) -->
                        <div class="absolute inset-0 bg-gradient-to-t from-gray-900 via-gray-900/60 to-transparent"></div>
                        
                        <!-- Content overlay (appears on hover) -->
                        <div class="absolute inset-0 bg-gray-900/95 backdrop-blur-sm p-6 flex flex-col justify-between opacity-0 group-hover:opacity-100 transition-opacity duration-300 z-20">
                            <div>
                                <h3 class="text-2xl font-bold text-white mb-3">
                                    {project.title}
                                </h3>
                                <p class="text-gray-300 mb-4 leading-relaxed text-sm">
                                    {project.description}
                                </p>
                            </div>
                            
                            <div>
                                <!-- Technologies -->
                                <div class="flex flex-wrap gap-2 mb-4">
                                    {project.tags.map((tag) => (
                                        <span class="px-3 py-1.5 bg-primary/20 border border-primary/30 text-primary rounded-lg text-xs font-medium">
                                            {tag}
                                        </span>
                                    ))}
                                </div>
                                
                                <!-- Link -->
                                {project.link !== "#" && (
                                    <a
                                        href={project.link}
                                        target="_blank"
                                        rel="noopener noreferrer"
                                        class="inline-flex items-center gap-2 px-4 py-2 bg-primary text-white rounded-lg font-semibold hover:bg-primary/90 transition-all duration-300 group/link"
                                    >
                                        Ver proyecto
                                        <svg class="w-4 h-4 transform group-hover/link:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
                                        </svg>
                                    </a>
                                )}
                            </div>
                        </div>
                        
                        <!-- Title always visible at bottom -->
                        <div class="absolute bottom-0 left-0 right-0 p-6 z-10">
                            <h3 class="text-xl font-bold text-white mb-2 group-hover:opacity-0 transition-opacity duration-300">
                                {project.title}
                            </h3>
                            <div class="flex flex-wrap gap-2">
                                {project.tags.slice(0, 2).map((tag) => (
                                    <span class="px-2 py-1 bg-gray-900/80 backdrop-blur-sm border border-gray-700/50 text-gray-300 rounded text-xs font-medium">
                                        {tag}
                                    </span>
                                ))}
                                {project.tags.length > 2 && (
                                    <span class="px-2 py-1 bg-gray-900/80 backdrop-blur-sm border border-gray-700/50 text-gray-400 rounded text-xs font-medium">
                                        +{project.tags.length - 2}
                                    </span>
                                )}
                            </div>
                        </div>
                    </div>
                </article>
            ))}
        </div>
    </div>
</section>
